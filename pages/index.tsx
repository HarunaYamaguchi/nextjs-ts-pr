import type { NextPage } from 'next';
import Layout from '../components/Layout';

const Home: NextPage = () => {
  return (
    <Layout pageTitle="Home">
      <h1>Works</h1>
    </Layout>
  );
};

export default Home;
